// tests/dateFormatter.test.ts

// Placeholder test – dateFormatter util sudah dihapus dari frontend
describe('dateFormatter utility', () => {
    test('placeholder', () => {
      // Tidak ada logic khusus yang perlu diuji di sini
      expect(true).toBe(true);
    });
  });
